import React from 'react'

function MovieGrid() {
	return (
		if (0 == 0)  {
				
				(
							<Spinner />
							
 )
} else {
	
	movies.map(item => (
		
		
		<MovieItem key={item.id} item={item} > 
					
				
				</MovieItem>


))
}
		)
}

export default MovieGrid
